╔════════════════════════════════════════════════════════════════════════════╗
║             🔍 GROUP NOTES CHECKBOX DEBUG INSTRUCTIONS                     ║
╚════════════════════════════════════════════════════════════════════════════╝

PROBLEM: Modal shows "2 selected" but checkboxes aren't visibly checked

────────────────────────────────────────────────────────────────────────────

📋 STEPS TO DEBUG:

1️⃣  Open Group-Notes.html in browser

2️⃣  Reproduce the issue (select 2 notes, see "2 selected")

3️⃣  Open browser Developer Tools:
    • Mac: Cmd + Option + J
    • Windows: Ctrl + Shift + J

4️⃣  Open the debug script file:
    GROUP-NOTES-CHECKBOX-DEBUG.js

5️⃣  Copy the ENTIRE contents of that file

6️⃣  Paste into Console tab and press Enter

7️⃣  Review the output - it will show:
    ✅ Global state (globalCheckedNoteIds, selectedNoteIds)
    ✅ DOM checkboxes (total, checked count)
    ✅ Individual checkbox states
    ✅ UI elements (batch bar, count display)
    ✅ Sync issues between state and DOM
    ✅ Render function checks
    ✅ Suggested fixes

────────────────────────────────────────────────────────────────────────────

🎯 WHAT TO LOOK FOR:

❌ SYNC ISSUES:
   • "DOM checked but NOT in globalCheckedNoteIds"
   • "NOT checked in DOM but IS in globalCheckedNoteIds"

❌ RENDER FUNCTION:
   • "Checks globalCheckedNoteIds: ❌"
   • "Sets checked attribute: ❌"

❌ STATE MISMATCH:
   • globalCheckedCount ≠ domCheckedCount
   • selectedCount ≠ domCheckedCount

────────────────────────────────────────────────────────────────────────────

💡 COMMON ISSUES & FIXES:

ISSUE 1: globalCheckedNoteIds stores numbers but DOM uses strings
FIX: Ensure consistent type checking (string vs number)

ISSUE 2: Checkboxes rendered before globalCheckedNoteIds is populated
FIX: Add checked attribute during render, not after

ISSUE 3: CSS hiding checkboxes (display: none, visibility: hidden)
FIX: Check computed styles in debug output

ISSUE 4: Event listeners not updating global state
FIX: Ensure updateSelectedCount() syncs properly

────────────────────────────────────────────────────────────────────────────

🔧 QUICK FIXES TO TRY IN CONSOLE:

Force sync checkboxes to global state:
────────────────────────────────────────
globalCheckedNoteIds.forEach(noteId => {
  const cb = document.querySelector(`.note-checkbox[data-note-id="${noteId}"]`);
  if (cb) cb.checked = true;
});
updateSelectedCount();


Clear everything and start fresh:
────────────────────────────────────────
globalCheckedNoteIds.clear();
selectedNoteIds.clear();
document.querySelectorAll('.note-checkbox').forEach(cb => cb.checked = false);
updateSelectedCount();


Manually add note to checked state:
────────────────────────────────────────
globalCheckedNoteIds.add('123'); // Replace 123 with actual note ID
const cb = document.querySelector('.note-checkbox[data-note-id="123"]');
if (cb) cb.checked = true;
updateSelectedCount();

────────────────────────────────────────────────────────────────────────────

📸 SEND ME THE OUTPUT:

After running the debug script, send me:
1. The console output (screenshot or text)
2. The returned summary object
3. Any sync issues listed

This will show EXACTLY what's wrong and I'll fix it immediately.

════════════════════════════════════════════════════════════════════════════
