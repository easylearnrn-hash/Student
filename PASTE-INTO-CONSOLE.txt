════════════════════════════════════════════════════════════════════════════
                    🔍 COPY THIS EXACTLY - PASTE INTO CONSOLE
════════════════════════════════════════════════════════════════════════════

(function() {
  const global = Array.from(globalCheckedNoteIds || []);
  const selected = Array.from(selectedNoteIds || []);
  const allBoxes = document.querySelectorAll('.note-checkbox');
  const checkedBoxes = document.querySelectorAll('.note-checkbox:checked');
  
  console.log('🔍 CHECKBOX STATE:');
  console.log('Global IDs:', global);
  console.log('Selected IDs:', selected);
  console.log('Total checkboxes:', allBoxes.length);
  console.log('Checked checkboxes:', checkedBoxes.length);
  
  console.log('\n📋 EACH CHECKBOX:');
  allBoxes.forEach((cb, i) => {
    const id = cb.dataset.noteId;
    const checked = cb.checked;
    const inGlobal = globalCheckedNoteIds.has(id);
    console.log(`[${i+1}] ID:${id} DOM:${checked} Global:${inGlobal}`);
  });
  
  console.log('\n✅ SUMMARY:');
  console.log('Match:', checkedBoxes.length === global.length ? 'YES' : 'NO');
  
  return {
    globalCount: global.length,
    selectedCount: selected.length,
    domTotal: allBoxes.length,
    domChecked: checkedBoxes.length,
    match: checkedBoxes.length === global.length
  };
})();

════════════════════════════════════════════════════════════════════════════
                    👆 COPY EVERYTHING ABOVE ��
════════════════════════════════════════════════════════════════════════════
